<li>
    <p>
        <!-- if user is logged in and (user logged in equal to
        who own the post OR user logged in equal to user who commented) -->

       <% if (locals.user &&(locals.user.id == post.user.id || (locals.user.id == comment.user.id))){ %>
           <small>
               <a href="/comments/destroy/<%= comment.id %>">X</a>
           </small>
       <% } %>
       <%= comment.content %>
       <br>
       <small>
           <%= comment.user.name %>
       </small>

       <small>
        <%= post.user.name %>
    </small>
    <br>
    <!-- Now in the front end side, if we are signed out , it would only show us count of like,
     would not allow to like the post or comments and if signed in allow us -->
    <small>
        <% if(locals.user){%>
            <a  class="toggle-like-button" data-likes="<%= comment.likes.length %>"  href="likes/toggle/?id=<%= comment._id %>&type=Comment">
                <%= comment.likes.length %> Likes
            </a>
            <%}else{%>
              <%= comment.likes.length %> Likes
              <%}%>

    </small>

   </p>

</li>